{
  "scripts": {
    "build": "wasm-pack build --no-pack --dev --target=web --out-dir=../pkg ./wasm",
    "release": "wasm-pack build --no-pack --release --target=web --out-dir=../pkg ./wasm",
    "postinstall": "wasm-pack build --no-pack --dev --target=web --out-dir=../pkg ./wasm"
  },
  "devDependencies": {
    "wasm-pack": "^0.13.1"
  }
}
